{
  "name": "quantum-fold",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Train CP1 Berry and Ricci calculations, explore a signed-area torus map, and reconstruct source-sheet lineages through folds and cusps.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
