{
  "name": "quantum-fold",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Explore a signed-area torus map, then fight through its local derivatives in a fold shooter.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
