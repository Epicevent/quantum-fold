{
  "name": "quantum-fold",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Move one BZ point, watch its Bloch image, and cross a signed-area fold.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
